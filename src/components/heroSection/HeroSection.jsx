import React from 'react'
import { container } from '../../classes'

const HeroSection = () => {
  return (
    <div className={`${container} Mmd:px-16 bg-purple-90`}>HeroSection</div>
  )
}

export default HeroSection